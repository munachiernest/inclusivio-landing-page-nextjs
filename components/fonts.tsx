import { Rethink_Sans } from "next/font/google";


export const RethinkSans = Rethink_Sans({
  subsets: ["latin"], // adjust subsets as needed
  // weight: ['400', '700'], // optionally specify weights if available
});
